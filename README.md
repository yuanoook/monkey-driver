# monkey-driver
Monkey driver is the best driver :D

```javascript

Labels-Node-Event

genLabels(node) // what is this
callNode(label) // who is being called

1. label be comprehensible
2. label be unique
3. node be accessible

4. viewport first
5. document second

logEvent(event) // what have been done
proposeAction(label) // what to do now
1. basics click/input
2. middle open/close
3. advanced login/out/approve


Scenario & Entropy
getScenario() => ({labels, entropy})
```
